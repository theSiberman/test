name: CI - Fast Feedback

on:
  pull_request:
  push:
    branches-ignore:
      - main

concurrency:
  group: fast-feedback-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quick-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Replace these placeholders with your real project commands.
      - name: Lint (placeholder)
        run: echo "Replace with lint command"

      - name: Typecheck (placeholder)
        run: echo "Replace with typecheck command"

      - name: Unit tests (placeholder)
        run: echo "Replace with test:quick command"

  changed-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Replace with your changed-files -> test selection logic.
      - name: Test Impact Analysis (placeholder)
        run: echo "Replace with test:changed command"
